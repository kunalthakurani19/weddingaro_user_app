import 'package:flutter/material.dart';

class AuthBGCommon extends StatelessWidget {
  const AuthBGCommon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Wrap(
      children: [],
    );
  }
}
